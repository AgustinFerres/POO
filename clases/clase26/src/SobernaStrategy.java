public class SobernaStrategy extends VacunaStrategy{

    public SobernaStrategy(String nombre, String origen, Integer cantidadDosis, String detalle) {
        super(nombre, origen, cantidadDosis, detalle);
    }
}
