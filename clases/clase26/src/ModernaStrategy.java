public class ModernaStrategy extends VacunaStrategy{

    public ModernaStrategy(String nombre, String origen, Integer cantidadDosis, String detalle) {
        super(nombre, origen, cantidadDosis, detalle);
    }
}
